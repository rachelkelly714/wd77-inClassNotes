//A literal is a Literal representation of avalue in JS. It is a fixed value
//a variable - they are literally provided in the code. 

//a variable is not a literal, in info assigned to the variable is the literal
      //(keyword)        (value/literal)
//ex: let placeholder = 'text'; 
//        (variable)   

/*STRING LITERAL
let city = 'Indianapolis';

//Numeric Literals
let num = 1

// BOOLEAN Literal
let isOn = true; 

OBJECT Literal 
let movie = {
    title: 'ELF',
    rating: 'PG',
    release: 'November 7th 2003'
};

ARRAY LITERAL

let daysOfWeek = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
*/ 
